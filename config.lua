Config = {}
Config.BoomBoom = {
    Controls        = {1, 51},  -- Go to https://docs.fivem.net/game-references/controls/ for more options.
    ControlsTitle   = "E",      -- What button the message tells the user to press (display text).
    Cooldown        = 15,       -- The length (in minutes) users must wait until using the command again.
}
